#dataset generation code

continuousProfiles <- ()

categoricalProfiles <- ()

buildContinuousDataSet <- function(k=2, size, dataRange = c(0, 15)){
  
}

buildCategoricalDataSet <- function(k=2, size, dataRange = c(1:5)){
  
}